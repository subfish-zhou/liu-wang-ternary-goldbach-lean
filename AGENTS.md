# 本项目当前工作约定

- 用户要求只保留一个主工作树和一个本地 `main` 分支。直接在此主工作树研究，不新建 worktree、分支、整库副本或外部项目工作目录。
- 用户已授权并行 Copilot CLI：仍只用本主工作树，各 CLI 仅新增或修改自己在 `LiuWang/Proof/Campaign20260915/<Owner>/` 的专属模块及指定任务记录。已有源码冻结；不得改其他工作者目录、Git 索引、分支或公共脚本。父级负责跨目录修改与验收接入。
- 编译一律经 `python3 scripts/build_local_closure.py <模块名>`，按单模块全局锁串行复用已核缓存。禁止另跑 `lake build`、直接 Lean/leanchecker、修改共享 packages 或重复提交同一构建。
- 从公开快照恢复，旧私有验收材料及运行日志已删除。不能把旧记忆中的通过状态冒充本轮验证。
- 数学目标严格遵循 Liu–Wang 原论文：每个满足 `Real.exp 3100 ≤ (N : ℝ)` 的奇自然数是三个奇素数之和，允许重复。不自拟替代路线，不降低常数、缩小参数域或默改疑点印式。
- 逐项核对当前源码中的生产者、消费者、定理参数与原文；同一上游数学输入去重，普通域条件不列为数学缺口。
- 零 `sorry` 不表示目标已证；标准 Lean 逻辑公理与未证明的数论前提分开说明。禁止新增未证明数学公理。
- 共享 `.lake/packages` 指向已有同工具链、同版本依赖；不得修改共享依赖。项目编译产物只写本树 `.lake/build`。
- 公开发布须另获授权；本轮本地恢复和研究不自动推送 GitHub。
