# 证明与验证边界

## 尚未完成的终点

`LiuWang.TheoremStatement` 仅定义目标；本快照不提供无条件主定理的完整证明。没有给出覆盖全部较小奇数并与 `exp(3100)` 衔接的完整证明。

## 已交付组件仍保留的输入

以下是重要边界举例，不是对全部数学缺口的穷尽计数：

- `CountCap` 已有严格窗与闭窗的实际计重上界，但仍要求同一原参数的 Gamma 高低估计及 RS1975 零点来源输入。
- `HeightCases` 的有限 K3 合账允许谱类别随高度改变，但仍保留原 T1 严格计数前提；其他消费者还保留 T7、零点尾项与实际短高度项。
- `LowHeight` 已将实际低段归约为原积分并完成 M1 的两情况合账；`7.6e-6` 的固定数值认证及全实参数域上界尚未完成。
- 无条件密度估计、例外零处理、各弧全部上游输入及主定理总装仍需继续完成。

共享模块已经导入，不表示其条件自动得到满足。局部证书与原积分的消费者关系也须区分。

## 源文核查

标记 `SOURCE_CHECK` 的印式保留待核状态。条件性四零计数终端从已证明的 (2.28) 与原参数直接推得，不使用或修改待核的 (2.29)；第三谱类的表台阶恒等式不冒称修复了 (2.37) 的印式或证明了其单独 `.00006` 数值预算。

## 发布检查范围

本地全量隔离重编及官方检查仍在运行；导出时的进度见 `verification/status.json`。既有分支与局部模块的检查不冒称当前公开快照的干净克隆全量构建已完成。

发布脚本校验源码文件哈希；目标定义的独立编译只证明该文件可检查，不证明所定义命题。没有发布原论文扫描件、内部审查报告或本机执行日志。
