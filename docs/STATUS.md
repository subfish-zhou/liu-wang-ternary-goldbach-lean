# 证明与验证边界

## 当前大数范围主根

`Parent.AllArcsPaid.threeOddPrimes_of_four_shared_inputs` 已消费全部弧预算，目标保持每个奇自然数 `N ≥ exp(3100)` 为三个奇素数之和。它仍带四项未证明的数学输入：

- M01：原 Theorem 7 全字符族计重零密度界；
- M04：实际 ζ 零点在 `16 < |Im(s)| < 1894438` 内的临界线覆盖；
- M06：所有整数 `q ≥ 3` 的原 totient 界；
- M07：原实零区域的小本原导子排除，供给导子至少 987。

完整量词与来源边界见 [gaps/ROOT-INPUTS-20260915.md](gaps/ROOT-INPUTS-20260915.md)。`LiuWang.TheoremStatement` 仍只是目标定义；本快照不提供其无条件证明，也未完成阈值以下的有限衔接。

## 本次接入的成果

- 第二弧：完整实际域的 `0.4012 N/L` 点态与局部能量积分界。
- 第三弧：同一逼近见证上的三个预算及 `0.5033 N/L` 点态、局部能量积分界。密度高度门和非实 Abel 抵扣均在真实消费者中处理；不假设未抵扣的旧积分已满足预算。
- 原零区域：实际尺度下的分类、实性、单性、唯一性与严格来源分支接线；没有把“无严格来源”误当成“闭窗口无零点”。辅助全 `x≥10` 原引理不因此宣布全域闭合。
- ζ：闭高度 16、整个开临界带的解析重数总和恰为 2；零点恰为一对简单的共轭临界线零点。原高度 1894438 的完整验证仍未完成。
- totient：保留已验有限 `log q≤294` 的全整数界（含例外整数223092870），并接入 `0<x≤1420` 的连续 θ 下界和相应标量比较。没有把这些有限结果冒充全域算术输入。
- 小导子：本快照纳入已父验的连续前缀至150及部分更大模数排除。活动中的更长前缀与自动并行队列不混入本次发布；未声称导子987输入已给出。

## 原文与来源核查

McCurley 第9页的 `8(0.9126+cos θ)^2(0.2766+cos θ)^2` 与当前多项式及精确系数相同；代码最初经讲义定位并不证明换用了另一个多项式。其余内部估计不因这一核对而宣称全部逐式复刻。

原小导子987结论由 McCurley 引用 Rosser 的未发表手稿；当前采用原指定实区域的独立可核验重证，未声称复刻该手稿。标记 `SOURCE_CHECK` 的字面中间式仍保留；整体预算足够不代表每条原印式都已核实或修订。

## 逻辑与检查范围

新父根的传递公理仅含 Lean 标准逻辑基础 `propext`、`Classical.choice`、`Quot.sound`；上述四个数论输入以定理参数显式保留，不在公理打印列表中。不能混淆这两类“前提”。

共同入口为 `LiuWang.Proof.Campaign20260915.Accepted`。本次闭包及源码绑定回执见 `verification/accepted-components-20260915.json`；这是局部新增编译与已核缓存复用，不是干净克隆全库构建验收。`verification/status.json` 是早期导出记录，不代表当前后台状态。

本次只发布显式清单内的已验源码和状态文件；不包含扫描文献、内部审查记录、凭据、运行日志、缓存或活动草稿。
