# 大数范围三素数主根：当前剩余两项输入

## 最新父验：M07 已从主根消去

`Parent.SmallConductorPaidRoot.threeOddPrimes_of_two_shared_inputs` 已消费完整 `SmallConductors.primitive_le_986_original_region`，不再含 `hsmall`。完整前缀是 `LiuWang.Proof.Campaign20260915.`。

请求 `d91eed644d604a188b7d28ae9ff12f87`：共同入口4894/4894通过，2项新增编译/官方精确重放、4892项绑定缓存复用；不是干净全库重建。生成的根签名与传递公理锥均已核对。

当前实际主根只剩：

1. **M04**：对所有实际ζ零点，在 `0<Re(s)<1`、`16<|Im(s)|<1894438` 上证明 `Re(s)=1/2`。
2. **M06**：对所有整数 `q≥3` 证明 `q/φ(q)≤ν(q)`，保持原ν及全部整数域。

M07的完整 `q≤986` 排除已由Lean有限分类及证书闭合，并进入真实例外源、原PaperSource、主弧和LocalC1消费者。原常数 `9.645908801`、`x≥10`、`q≤x` 及闭实零边界保持。McCurley/Rosser未发表手稿的出处审计仍为来源账，不再是此主根的数学输入。

主根公理仅含 `propext`、`Classical.choice`、`Quot.sound`；M04/M06仍是显式未证明数学参数，故尚非无条件主定理。目标仍限奇自然数 `N≥exp(3100)`；阈值以下的有限衔接单列未闭。

## 历史状态：三项输入（M07接入之前）

## 本地最新父验（本次检查点）

`Parent.DensityPaidRoot.threeOddPrimes_of_three_shared_inputs` 已实际调用
`Density.source_T7`，从原三素数总根消去 `hdensity`。完整定理名以
`LiuWang.Proof.Campaign20260915.` 为前缀。

请求 `744234d8f72047689a904dcff2eeb856`：2419/2419 通过，1个新增编译及官方精确重放、2418个绑定缓存复用。父级读取生成的完整定理签名，剩余数学参数恰为：

1. **M04**：原有限高度 ζ 零点验证。
2. **M06**：所有 `q≥3` 的原 totient 界。
3. **M07**：原实零区域的小本原导子排除。

M01 已核收并进入实际主根，不再计为未付数学输入：保留全正模数、全部普通字符、解析重数、原高度门和完整 `1/2≤alpha<1`。证明从真实解析输入给出原不变的 Theorem 7 上界，没有新增均方、最大值、边界非零等假设。

Chen 英文印式的 S01 来源核查仍独立保留。局部逐字标量比较不相容的证书不表示密度定理为假，也没有据此指定作者原意。未声称全部中间印式逐字复刻。

新主根公理锥只有 `propext`、`Classical.choice`、`Quot.sound`，但上述三项数学前提仍以定理参数存在，故主定理尚非无条件闭合。目标仍是奇自然数 `N≥exp(3100)`；阈值以下有限衔接不在本次范围内。

本地回执：`.lake/campaign-20260915/density/parent-root-acceptance.json`。下文保留上一公开快照的四输入说明，属于历史状态。

## 历史公开快照：四项实际剩余输入


本文件记录本次源码快照所包含的已核接线。目标是：每个满足 `Real.exp 3100 ≤ (N : ℝ)` 的奇自然数 N，均为三个奇素数之和（允许重复）。不是全部奇数的无条件结论，也不包含有限区间衔接。

## 已检查的父根

`LiuWang.Proof.Campaign20260915.Parent.AllArcsPaid.threeOddPrimes_of_four_shared_inputs`

源码：`LiuWang/Proof/Campaign20260915/Parent/AllArcsPaid.lean`。

请求 `93846317a16b441496cd95f011109e6e`：2209 个本地依赖模块通过，一项新编译与官方精确重放、2208 项绑定缓存复用；不称全库干净重建。

本父根已经消费：
- 严格例外源有/无两分支及普通低零预算；
- 第二弧原 0.4012 点态和局部能量积分界；
- 第三弧原 0.5033 点态和局部能量积分界（同一 q,a,eta 的三个预算）；
- 已有主弧、第四弧、统一能量预算与素数幂剔除。

主根不再带 `hsecond`、`hthird`、来源存在性、普通低零预算或独立 T1 计数假设。下述四个数学输入仍显式存在，绝不能因 `#print axioms` 只含标准逻辑公理就称主定理无条件成立。

## 四项剩余输入

1. **M01：原 Theorem 7 全字符族计重零密度界。** 对每个正模数 q，在原高度门 `y ≥ max(100000/q,10000 log q)` 及 `1/2 ≤ alpha < 1` 上，给出实际 `familyCount` 的原 `sourceSevenBound`。同一输入供第二弧、第三弧及主弧使用。
2. **M04：原有限高度 ζ 零点验证。** 对全部实际 ζ 零点，`0<Re(s)<1` 且 `16<|Im(s)|<1894438` 时须有 `Re(s)=1/2`。闭高度16已由实际完整计重、共轭对及单性证明覆盖；不是用一个样本替代高处全族。
3. **M06：原整数 totient 界。** 对所有整数 q≥3，须证明 `q/φ(q)≤ν(q)`；其中 φ 是 Euler totient 函数，`ν(x)=exp(γ) log(log x)+2.50637/log(log x)`，γ 是 Euler–Mascheroni 常数。已有截口运输引理把这一份输入供给全部消费者，不重复要求每弧证明一遍。
4. **M07：原实零区域中的小本原导子排除。** 主根以等价的导子下界形式接受输入：对实本原非主字符、`x≥10`、`q≤x`、`beta≥1-1/(9.645908801 log x)`，若实际 L 函数在 beta 为零，则 q≥987。它统一供给主弧例外源与第三弧低零端口；不假定整个 `LocalC1Data`。

## 与旧18项账目的对应

主定理实际依赖口径：5项原工作包闭合；8项独立预算障碍消去；M05在全部实际应用尺度的分类/分支输入已消去；剩余4项共享上游。18个原编号已经程序核对为互不重叠的完整划分。

这不表示每个旧中间接口或每一条原文印式都已复刻。辅助全 `x≥10` 零区域、既有标记为 `SOURCE_CHECK` 的字面中间式，保留在来源审计账；它们不能再与主根实际四项输入混为同一个缺口数。

第三弧集成来源：`Parent/RecoveredThirdArc.lean`，已绑定核对其5个公开定理的源码、对象、编译/重放日志及公理集合。其实际终端不含独立 M04 参数；M04 仍在整个主根的其他消费者中需要。

所有未证数论输入都以定理参数明确保留；未新增数论公理。当前验收证据保存在本地 campaign 的 `parent-all-arcs-acceptance.json`。
